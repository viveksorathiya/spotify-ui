import React from 'react'
import RESOURCES from '../../constants/resources'

const NavButton = ({isBack}) => {
    return (
        <div className="nav_button">
            <img src={isBack: RE} alt="" />
        </div>
    )
}

export default NavButton
