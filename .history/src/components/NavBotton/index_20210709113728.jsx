import React from 'react'
import RES

const NavButton = ({isBack}) => {
    return (
        <div className="nav_button">
            <img src={isBack: } alt="" />
        </div>
    )
}

export default NavButton
